import { View } from 'react-native';

const MenuScreen = () => {
    // This component is a placeholder to satisfy expo-router's file-based routing.
    // The tab button's press event is intercepted in _layout.tsx to toggle the drawer,
    // so this screen will never actually be displayed.
    return <View />;
};

export default MenuScreen;
